import fs from 'node:fs';

const path = 'web/src/workout-picker-v2.js';
let source = fs.readFileSync(path, 'utf8');

if (source.includes('function appendDemoIframe(panel, src, title)')) {
  console.log('Drive demo patch already applied.');
  process.exit(0);
}

const before = `function createDemoMediaPanel(item, media) {\n    const panel = document.createElement('div');\n    panel.className = 'picker-demo-panel';\n    panel.dataset.pickerDemoPanel = item.motion;\n\n    const showError = () => {\n        panel.innerHTML = '';\n        const message = document.createElement('div');\n        message.className = 'picker-demo-error';\n        message.textContent = '示範媒體載入失敗';\n        panel.appendChild(message);\n    };\n\n    if (media.type === 'youtube') {\n        const iframe = document.createElement('iframe');\n        iframe.title = \`\${item.motion} 動作示範\`;\n        iframe.loading = 'lazy';\n        iframe.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture';\n        iframe.referrerPolicy = 'strict-origin-when-cross-origin';\n        iframe.setAttribute('allowfullscreen', '');\n        iframe.src = media.src;\n        panel.appendChild(iframe);\n        return panel;\n    }\n\n    if (media.type === 'video') {\n        const video = document.createElement('video');\n        video.controls = true;\n        video.playsInline = true;\n        video.preload = 'metadata';\n        video.setAttribute('aria-label', \`\${item.motion} 動作示範\`);\n        video.addEventListener('error', showError, { once: true });\n        video.src = media.src;\n        panel.appendChild(video);\n        return panel;\n    }\n\n    const image = document.createElement('img');\n    image.alt = \`\${item.motion} 動作示範\`;\n    image.loading = 'lazy';\n    image.decoding = 'async';\n    image.addEventListener('error', showError, { once: true });\n    image.src = media.src;\n    panel.appendChild(image);\n    return panel;\n}\n`;

const after = `function appendDemoIframe(panel, src, title) {\n    const iframe = document.createElement('iframe');\n    iframe.title = title;\n    iframe.loading = 'lazy';\n    iframe.allow = 'accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; fullscreen';\n    iframe.referrerPolicy = 'strict-origin-when-cross-origin';\n    iframe.setAttribute('allowfullscreen', '');\n    iframe.src = src;\n    panel.appendChild(iframe);\n    return iframe;\n}\n\nfunction configureLoopingDemoVideo(video, label) {\n    video.controls = true;\n    video.autoplay = true;\n    video.muted = true;\n    video.defaultMuted = true;\n    video.loop = true;\n    video.playsInline = true;\n    video.preload = 'metadata';\n    video.setAttribute('aria-label', label);\n}\n\nfunction createDemoMediaPanel(item, media) {\n    const panel = document.createElement('div');\n    panel.className = 'picker-demo-panel';\n    panel.dataset.pickerDemoPanel = item.motion;\n    const label = \`\${item.motion} 動作示範\`;\n\n    const showError = () => {\n        panel.innerHTML = '';\n        const message = document.createElement('div');\n        message.className = 'picker-demo-error';\n        message.textContent = '示範媒體載入失敗';\n        panel.appendChild(message);\n    };\n\n    if (media.type === 'youtube') {\n        appendDemoIframe(panel, media.src, label);\n        return panel;\n    }\n\n    if (media.type === 'drive') {\n        const video = document.createElement('video');\n        configureLoopingDemoVideo(video, label);\n        video.addEventListener('error', () => {\n            panel.innerHTML = '';\n            if (media.fallbackSrc) appendDemoIframe(panel, media.fallbackSrc, label);\n            else showError();\n        }, { once: true });\n        video.src = media.src;\n        panel.appendChild(video);\n        return panel;\n    }\n\n    if (media.type === 'video') {\n        const video = document.createElement('video');\n        configureLoopingDemoVideo(video, label);\n        video.addEventListener('error', showError, { once: true });\n        video.src = media.src;\n        panel.appendChild(video);\n        return panel;\n    }\n\n    const image = document.createElement('img');\n    image.alt = label;\n    image.loading = 'lazy';\n    image.decoding = 'async';\n    image.addEventListener('error', showError, { once: true });\n    image.src = media.src;\n    panel.appendChild(image);\n    return panel;\n}\n`;

if (!source.includes(before)) {
  throw new Error('createDemoMediaPanel patch anchor not found');
}
source = source.replace(before, after);
fs.writeFileSync(path, source);
